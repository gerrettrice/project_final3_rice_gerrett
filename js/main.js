$(document).ready(function () {

    // Call SMINT jQuery Plugin
    $('.subMenu').smint({
        'scrollSpeed': 1000
    });

    //Call Text Rotate
    $('.demo3 .rotate').textrotator({
        animation: 'flipCube',
        speed: 1500
    });
});
